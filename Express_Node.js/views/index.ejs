<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Mon Application</title>
  <!-- LIEN GOOGLE FONTS -->
  <link href="https://fonts.googleapis.com/css?family=Neucha" rel="stylesheet">
</head>
<!-- AJOUT DES STYLES DIRECTEMENT DAN LE HTML -->
<body style="margin: 0 auto; text-align: left; text-decoration: none; width: 220px; margin-top: 10%; font-family: 'Neucha',cursive;">
  Node.js, Express et MongoDB.
  <!-- formulaire pour ajouter nouveau document -->
    <form action="/adresse" method="POST" style="margin-top: 10%;
    background-color: skyblue;
    margin-bottom: 10%;
    padding: 17px;">
    <input type="text" placeholder="nom" name="nom">
    <input type="text" placeholder="prenom" name="prenom">
    <input type="text" placeholder="Telephone" name="telephone">
    <input type="text" placeholder="ville" name="ville">
    <input type="text" placeholder="codepostal" name="codepostal">
    <button type="submit" style="margin: 12%;">Submit</button>
  </form>

  <!-- affiche le contenu de adresse dans des li-->
  <ul class="adresse" style="background-color: aliceblue;
    padding: 41px; border: skyblue dotted 5px;">
  <% for(var i=0; i<adresse.length; i++) {%>
    <li class="client">
      <span><%= adresse[i].nom %></span>
      <span><%= adresse[i].prenom %></span>
      <span><%= adresse[i].telephone %></span>
      <span><%= adresse[i].ville %></span>
      <span><%= adresse[i].codepostal %></span>
    </li>
  <% } %>
  </ul>

</body>
</html>